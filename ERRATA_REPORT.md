# 《电动力学》自学教材 · 独立复核与勘误报告

> **复核对象**：`electrodynamics_textbook_v2.tex`（8928 行）与编译产物 `electrodynamics_textbook_v2.pdf`
> **复核日期**：2026-09-21
> **复核方式**：8 名审查员分工逐行精读 + 队长独立复核；数值与符号结论一律用
> Python（sympy 1.14 / numpy 2.5 / scipy 1.18）独立复算；
> 结构类结论直接检验编译产物（PDF 对象、可折叠图层、书签、页码）。
> **结论**：共确认约 **140 条**问题（含 2 条 blocker、20 条 high）。
> 本轮**已修正 18 条**（含全部 blocker 与关键 high），其余按分级列出待修。

---

## 一、复核方法（为什么这次结论可信）

本次复核针对的是**上一轮报告本身可信度不足**的问题，因此在方法上做了三项硬约束：

1. **不采信仓库自述。** `README.md` / 旧 `errata_report.md` / `CHANGELOG.md` 中的
   「已修正」「已统一」等声明一律**重新独立验证**。实测发现 v2.15 报告称源文件 8838 行，
   而实物行数与之不符，说明该报告与实际状态已脱节。
2. **数值与符号结论必须复算。** 每个复核结论都附可重跑的 Python 脚本（见 `tools/audit/`）；
   符号恒等式用 sympy 验证，数值结论用独立数值导数或多路径交叉验证。
3. **可证伪性优先。** 每条问题至少给出一个**独立判据**（反例、量纲、极限、物理自洽性、
   与同章其他表述的冲突），而不是「与某教材不一致」。

复核中还发生过一次**误报**，一并记录以示方法有效：最初报出「速查表 `B = μ(H+M)` 与正文
`B = μ₀(H+M)` 矛盾」并定为 high。用 Python 打印该行精确文本后确认原文是 `\muz` ——
导言区定义的宏，展开即 `\mu_0`，**该行本来就正确**。根因是未展开自定义宏就读源码。
该条已撤回，并把「凡涉及自定义宏必须先展开」写入 `STYLE_GUIDE.md`。**假发现比漏报更有害。**

---

## 二、v2.15 自述修正项的独立复核结果

v2.15 声称修正 29 项。本轮对其中的**关键 8 项**做了独立复核，结论：**全部正确**。

| v2.15 修正项 | 独立复核判据 | 结论 |
|---|---|---|
| 介质-真空界面束缚电荷符号（§3.3.3） | 用 `ε₀(E₂z−E₁z) = σ_b` 交叉检验，反号恒不成立；与电容器例题、包覆导体球例题三处符号一致性验证 | ✓ 正确 |
| 超导球外磁场系数与符号 | 三个独立判据：`B_r(R)=0` 对任意角恒成立、赤道 `B=(3/2)μ₀H₀`、`B = −μ₀∇φ_m` 逐分量相等 | ✓ 正确 |
| 达朗贝尔算符 □ 全书统一 | 抽查全书 **9 处**，无任何残留矛盾写法 | ✓ 正确 |
| 交流电容器坡印廷功率 | 积分面 `2πRd` 正确（端面无径向能流）、`dU/dt = −P` 成立、局部守恒 `∂ₜu+∇·S=0` 逐点校验通过 | ✓ 正确 |
| 电偶极辐射功率数值 | `μ₀p₀²ω⁴/12πc` 独立复算得 **17.33 W**（原 1.7×10⁻⁸ W 差 10⁹）；1 km 处 ⟨S⟩ = 1.38×10⁻⁶ W/m² | ✓ 正确 |
| 第二不变量 εFF 系数 | 按本书约定 `F^{0i}=−E_i/c`、`ε₀₁₂₃=+1` 逐项展开，`εFF = +8E·B/c`，`I₂ = ⅛εFF`；全书 6 处已同步 | ✓ 正确 |
| `F^{i0}` 符号 | 独立验算 `F^{i0}=+E^i/c`，由 `∂_iF^{i0}=μ₀cρ` 推出 `∇·E=ρ/ε₀` 链条成立 | ✓ 正确 |
| TE₁₀ 表面电流分布叙述 | 用本章自给的 `H_x`、`H_z` 独立验算 `K = n̂×H`，宽边中央确为纯纵向且最大 | ✓ 正确 |

同时确认高斯定理/泊松方程符号在全书 **13 处**（含例题、自测、答案速查、对比表）**全部正确**，
历史性的符号错误已修复且未复发。

**这说明上一轮修正不是纸上功夫。** 但本轮仍在同一批「已修正过」的区域之外找到了新问题。

---

## 三、本轮确认的问题（按严重度）

### 3.1 blocker（2 条，均已修正）

#### CH10-01 · 第10章对同一个不变量给出两个相反的值

第10章存在**重复定理**（label `lorentz-invariants`），把第一不变量写成
`𝒥₁ = F_μνF^μν = 2(E²/c² − B²)`，而本章其余 5 处（前述定理、逐项展开推导、
核心公式小结、三维/四维对照表、附录）**一致**为 `2(B² − E²/c²)`——恰好是它的负值。

更严重的是紧随其后的结论表**整体反向**：按正确的不变量 `I₁ = B² − E²/c²`，应是
「`I₁>0`（cB>E，磁场占优）⇒ 可消去电场」，而原文写的是「`I₁>0` ⇒ 使 `B=0`」，
三条结论全部颠倒，与同章前述洞见直接冲突。

**已修正**：重复定理改回与全书一致的约定（带 ½ 系数、取 `B²−E²/c²`），
结论表按正确逻辑重写，并加注与本章定义式的交叉引用。

#### UI-01 · 配套站点单位换算器 5 个因子错量级（本轮重建时引入）

站点重建时自行推导的 SI⇄高斯换算因子有 5 个错误：
`E` 小 10⁶ 倍；`D` 大 100 倍；`P` 既错 4π 又错 100 倍（**高斯制中 P 不带 4π**，
不能与 D 合并）；`H` 取了倒数（错 6332 倍）；`M` 错 79577 倍。
这会让读者用工具核对作业时得到完全错误的数值。

**已修正**：按基本定义重新推导全部 8 个因子，并加入**加载时自检**（任一因子偏离预期即
在控制台报错）。独立验证用了两条互相独立的路径：电荷因子由「两 1 statC 电荷相距 1 cm
斥力 1 dyn」定出，与 CODATA 的 `e = 4.803205×10⁻¹⁰ esu` 比对比值 **1.000000**；
再用 `σ = 1 C/m²` 的无限大带电面做物理自检，「SI 算完再换算」与「高斯制直接 `4πσ`」
两条路径比值 **1.00000000**。校验脚本：`tools/audit/verify_units.py`。

### 3.2 high（20 条，本轮修正 10 条）

**已修正：**

| ID | 位置 | 问题 | 判据 |
|---|---|---|---|
| OCG-01 | §7.1.5（p.103） | 整节（含**小节标题**）被包在默认关闭的可折叠图层内；该节含洛伦兹规范**定义**、两规范**对比表**与**达朗贝尔方程** | 读者按目录翻到该页只见「展开推导」按钮、无小节标题；PDF 书签证明编号本身正常 |
| OCG-02 | 全书 17 个图层 | 全部缺 `/Usage` 字典且默认关闭 ⇒ **打印/导出丢失** 10 组自测答案、6 个挑战题块与 §7.1.5 | 按 PDF 规范 ISO 32000-1 §8.11.4.3，无 `/Usage /Print` 时按当前可见状态打印 |
| CH08-01 | TE₁₀ 场分量 | `H_z` 符号错（`+i` 应为 `−i`） | sympy 相位量代数：印出的三分量不能同时满足 Faraday 与 Ampere；反解之比恰为 −1 |
| CH08-03 | 洛伦兹色散 | `dn/dω` 漏掉决定符号的因子 ⇒ 恒正，**无法得出**同段要求的反常色散 | 数值扫描：原式在 ω∈(0,3] 最小值 1.0×10⁻³（恒正）；`Re√(1+χ)` 的数值导数在共振处确为负 |
| CH05-01 | 铁磁平面例题 | 把 `H` 的法向性误记为「`B` 只有法向分量」，与同句 `H_1t=H_2t=0` 自相矛盾 | 镜像电流复算：`I′=+I` 才使 `H_t=0`；但真空区内 `B_x ≠ 0` |
| APX-01 | 附录 D | 电荷换算 `q_SI = q_G/√(4πε₀)` 方向与数值双错，且同句自相矛盾（`1/√(4πε₀)=9.48×10⁴` vs 同句 `3.3356×10⁻¹⁰`） | `3.3356×10⁻¹⁰` 与附录 C 的 `e = 4.803×10⁻¹⁰ esu` 完全自洽 |
| CH01-01 | 章首学习路径 | 引用的小节号 **8 处全错**，出现在全书第一个导航面板上 | 依据编译产物实测锚点逐条核对 |
| CH01-02 | §1.4 洞见 | 正电荷处电势高低**说反了**，与紧邻上一段自己的判据冲突 | 解析解 `φ=q/4πε₀r`：`∇²φ=−qδ³(r)/ε₀`，r→0 时 φ→+∞ |
| CH01-03 | §1.6.2 | 复合 δ 函数定理条件写成「单调可微」，应为「只有单根」；与本章例题所用的 `x²−a²` 冲突 | sympy 展开 `DiracDelta(x²−a²)` 成立，而 `x²−a²` 显然非单调 |
| UI-03 | 站点无障碍 | 勘误徽标对比度 4.08:1，低于 WCAG AA 的 4.5:1 | 按 WCAG 2.1 相对亮度公式实测；修正后 blocker 7.85 / high 5.77 / medium 5.59 / low 5.89（`tools/audit/verify_contrast.py`） |

**待修正（最高优先级）：**

| ID | 位置 | 问题 | 修正方案 |
|---|---|---|---|
| **CH07-01** | §7.4.3 动量守恒 | 写成 `∂g_i/∂t + ∂_jT_ij = −f_i`，与同节 `T` 定义不配套。自由平面波代入后给出 `f ≠ 0`，等于说**真空对自身施力**；静电场带电面片判据也反号。与同节「`T_zz = −ε₀E²`、负号即辐射压强」冲突 | 把 `+∂_jT_ij` 改为 `−∂_jT_ij`（**一个字符**），不要改 `T` 的定义（否则与本节例题冲突） |
| **CH09-03** | 第9章 | 两处承诺「天线方向性」、随堂自测考查 `F(θ,φ)`，但全书**从未定义** `F(θ,φ)`，第9章也无天线小节 ⇒ 该题无法作答 | 补一小节给出 `F(θ,φ)` 定义，或删去该自测题与相关承诺 |
| **BEGIN-01** | §4.3 | 格林函数法被写进章目标、框架、自测与误区，但**全书无推导无例题**；误区让读者去第1章，而第1章只有自由空间格林函数 | 三选一：新增 §4.4（约 2 页）／降级目标措辞／删自测题 |
| **BEGIN-02** | §4.4 | 柱坐标贝塞尔：定理名为「通解」却不给通解，全章无例题用它，自测却考它，且 9 个附录无特殊函数表 | 补通解与一道例题，或把该定理降级为「结论」并删自测题 |
| **CH08-02** | 波导例题 | 「结果解读」的简并判断错：`b=a/2` 时 `f_c,01 = c/(2b) = 6 GHz ≠ f_c,10 = 3 GHz`；真正简并的是 TE₀₁ 与 TE₂₀。且与同段「工程上常取 2.5 cm」矛盾，「避开 b=a/2」与标准实践（`b ≤ a/2` 才有倍频程单模带宽）相反 | 重写该段结论 |
| **CH09-01** | 磁偶极例题 | 题解相位 `sin → cos`，与题设 `cos ωt`、定理与题1解析三处不自洽 | 统一相位约定 |
| **CH10-02/03** | 交叉引用 | 章节交叉引用错误 | 按实际章节号修正 |
| **附录 H** | p.170 | 答案速查仍有缺口：第1章漏选择题 4-B 且判断题编号错位（应 5/6/7）、第6章 2 道填空缺失、第9章 2 道计算题无栏 | 补齐并校正栏位 |

### 3.3 medium / low（约 118 条）

涵盖：推导跳步、交叉引用错误、记号不一致（`ρ_p`/`ρ_b`、`J_m`/`J_M`、`K_Mb` 未定义）、
术语与符号约定冲突、图示标注、量纲校验缺失或指数写错、附录常数有效数字等。

---

## 四、初学者自学适用性（本书核心卖点，此前从未被系统评估）

以「具备大学基础物理与多元微积分、但未学过电动力学」的读者视角通读全书，结论：

**做得好的地方（应保留）：**

- **练习闭环完整**：10 章 × 5 道综合练习题共 **50 道**，全部配 `[分析] + [解答] + [量纲校验]`
  三段式，逐章核对 10/10 通过。
- **「显然/易得」式跳步几乎为零**：全书「显然」仅 1 处且属修辞性用法——
  这对一本 AI 生成的教材而言相当难得。
- **误区质量高**：17 个「常见误区」中 12 个属高质量（针对真实错误并给出正确做法）。
- **后半程例题的写法是本书最强教学设计**：带「结果解读」与「极限校验」
  （如点电荷与导体平面、铁磁-真空界面、同轴电缆能量传输、波导尺寸设计）。

**必须补的缺口：**

1. **第4章两个方法「承诺未交付」**：格林函数法与柱坐标贝塞尔（见 3.2 表）。
2. **第7章起缺复指数/时谐约定声明**：从 `e^{i(k·r−ωt)}` 首次出现处（含复介电常数、
   纯虚波数、时间平均功率）都需明确约定，否则自学者分不清复振幅与瞬时值。
   建议在首次使用前插一个 5 行的约定框。
3. **例题断崖**：第8章 13 页仅 **1 道**例题；第9章 2 道且全为「挑战」；
   第6章 2 道全为「提高」。相比之下第1章 15 道。建议按报告补 9 道题。
4. **前置知识缺口**：勒让德多项式正交性、贝塞尔函数、格林函数、张量指标运算等
   首次使用时未预先引入。
5. **打印丢失答案**（已由 OCG-02 修正后消失）：原状态下打印版读者拿不到自测详解，
   练习闭环对纸质读者是断裂的。

---

## 五、配套站点（`html/`）

### 5.1 原站点的问题

原 `html/` 六页面**描述的是一本不同的书**：

| | 原 html 声称 | 实物 |
|---|---|---|
| 章节目录 | 矢量分析／静电场／静磁场／麦克斯韦方程／电磁波／**传输线**／**波导**／辐射／相对论／**等离子体** | 数学预备知识／静电学基础／电介质与静电边界条件／静电学边值问题／静磁学／磁介质／麦克斯韦方程组／电磁波的传播／电磁波的辐射／相对论电动力学 |

真实教材**没有**「传输线」「波导」「等离子体」独立章。连带后果：

- **PDF 阅读器完全不可用**：`<iframe src="about:blank">` 从未加载 PDF，
  「上一章/下一章」只改标签不换内容，侧栏「易错点」4 条全是
  `alert('该章节正在整理中...')`。
- **勘误页是虚构数据**：6 条勘误（第2章 p.47 式(2.31)、第7章 p.245 式(7.8)、
  附录 H p.382 等）**全部无法与实物对应**——无「传输线」章、附录仅到 I 且 p.382 超出总页数。
  真实的 v2.15 勘误 29 项**一条都没有**。
- **学习路线指向不存在的章**：`index.html` 中「工科路线跳过第10章（等离子体）」指向不存在的章；
  `reader.html` 中 `route === 'engineering'` 分支是**空代码块**。
- 六页面各自内联一份重复 CSS，无共享样式表。

### 5.2 重建结果

按**编译产物实测的真实目录**重建：新增共享设计系统 `assets/site.css`
（设计令牌、明暗双色、打印样式、WCAG AA 校核）与 `assets/site.js`（渐进增强），
导航数据由 `book-data.js` 提供——该文件由 `tools/site/export_site_data.py`
从 `.toc` **自动生成**，不手写，因此不会再与教材脱节。

页面结构：`index`（实测规格 + 完整目录）、`reader`（真实目录 + 可用的 PDF 定位）、
`roadmap`（按前置依赖排序的 10 章路线 + 进度勾选）、`formulas`（速查，含适用条件注）、
`tools`（单位换算 / 电磁波参数 / 矢量运算）、`errata`（本报告的数据化版本）。
`appendix-nav.html` 转为跳转页以保全旧链接。

验证：7 页面链接零断裂、无内联样式块、无内联事件处理器、全部 JavaScript 通过
`node --check`、对比度实测全部 ≥ 4.5:1。

---

## 六、修正后的编译验证

```
latexmk -xelatex -interaction=nonstopmode electrodynamics_textbook_v2.tex
```

| 指标 | 修正前（v2.15） | 修正后 |
|---|---|---|
| 页数 | 172 | **172** |
| 错误 | 0 | **0** |
| Overfull hbox | 0 | **0** |
| Underfull hbox | 0 | **0** |
| 未定义引用 | 0 | **0** |
| 缺失字形 | 0 | **0** |
| OCG 图层 `/Usage` | **17/17 缺失** | **17/17 存在，PrintState=/ON** |

> 修正过程中曾一度引入 3 处 `Undefined control sequence`（误用了未定义的 `\vecH`/`\vec0`）
> 与 1 处 Overfull（速查表行内长公式无法断行）。二者均已修正并复验清零。
> 这恰好印证了本书导言区宏体系的坑：`\vecH` 并不存在，只有 `\vecE`/`\vecB`/`\vecK`/`\vecJ`/`\vecA`。

---

## 七、给后续维护者的建议

1. **速查表必须与正文同步维护。** 本轮在速查表发现的 4 条问题中有 3 条属于
   「正文对、速查表错」——根因是速查表由正文手工摘录而成。
   已在 `STYLE_GUIDE.md` §3.4 增加对应规则。
2. **凡涉及自定义宏的判断，必须先展开宏。** 本书导言区定义了 `\muz`(=`\mu_0`)、
   `\eps`、`\vecE` 等一批宏，纯文本阅读源码极易误读（本轮已因此产生一次误报）。
   已在 `STYLE_GUIDE.md` §3.4 补充说明。
3. **不要把章节标题放进可折叠内容**（已列入 §3.3 禁止事项）。
4. **改题时同步维护附录 H 答案速查表**（该表历史上已两次出现缺漏与错栏）。
5. **`\begin{foldable}` 环境实测从未被使用**，属残留代码，建议清理。

---

*本报告与配套站点 `html/errata.html` 内容一致；该页面由结构化数据渲染，便于后续增补。*
*可复跑脚本见 `tools/`，审查纪律与实测基线见 `tools/audit/AUDIT_PROTOCOL.md`。*
